---
title: Location Picker
description: A cross-platform location picker component for Web and React Native that allows users to search and select locations with geolocation support.
---

import { Code, Package, ListTree, Zap } from "lucide-react";
import LocationPickerPreview from "@/components/previews/location-picker";
import { LocationPickerAdvancedExamples } from "@/components/previews/examples/location-picker-advanced";

## Usage

<Tabs items={["Web", "React Native"]}>
  <Tab value="Web">
  <Tabs items={["Preview", "Code"]}>
    <Tab value="Preview">
      <LocationPickerPreview />
    </Tab>
    <Tab value="Code">
      ```tsx
import { LocationPicker } from "@/components/location-picker"

export default function Home() {
        return (
          <LocationPicker />
        )
}
  ```
    </Tab>
    </Tabs>
  </Tab>
  <Tab value="React Native">
    ```tsx
import { LocationPicker } from "@/components/location-picker-rn"

export default function Home() {
      const [selectedLocation, setSelectedLocation] = useState<string>("");

      return (
        <LocationPicker
          variant="popover"
          onChange={setSelectedLocation}
          placeholder="Tap to search locations..."
        />
      )

}
```

  </Tab>
</Tabs>

## Installation

<p className="md:text-sm  text-gray-700 dark:text-gray-300 max-w-3xl">
  Install the Location Picker component for your platform using your preferred
  package manager.
</p>

<Tabs items={["Web", "React Native"]}>
  <Tab value="Web">
    ```npm
    npx shadcn@latest add @rigidui/location-picker
    ```
  </Tab>
  <Tab value="React Native">
    ### Prerequisites [!toc]

    First, ensure you have React Native Reusables properly set up in your project. Follow the complete setup guide at [React Native Reusables Installation](https://reactnativereusables.com/docs/installation).

    ### Install Dependencies [!toc]

    Install the required Expo dependencies for location functionality:

    ```bash
    npx expo install expo-device expo-location
    ```

    ### Configure Expo Plugins [!toc]

    Add the `expo-location` plugin to your `app.json` file:

    ```json
    {
      "expo": {
        "plugins": ["expo-location"]
      }
    }
    ```

    For additional configuration options and permissions setup, refer to the [Expo Location documentation](https://docs.expo.dev/versions/latest/sdk/location/).

    ### Install Component [!toc]

    ```npm
    npx shadcn@latest add @rigidui/location-picker-rn
    ```

  </Tab>
</Tabs>

## Features

<p className="md:text-sm mb-4!  text-gray-700 dark:text-gray-300 max-w-3xl">
  The Location Picker component is packed with powerful features designed to
  provide an exceptional user experience across both Web and React Native
  platforms.
</p>

<Cards>
  <Card title="Location Search">
    Users can search for locations by name, address, or postal code with
    real-time suggestions.
  </Card>
  <Card title="Dual Variants">
    Choose between popover variant for compact layouts or inline variant for
    full-featured forms.
  </Card>
  <Card title="Autocomplete Suggestions">
    As users type, the component provides autocomplete suggestions for locations
    with debounced search.
  </Card>
  <Card title="Cross-Platform Support">
    Available for both Web and React Native with platform-specific optimizations
    and native geolocation support.
  </Card>
  <Card title="Geolocation Support">
    Allows users to quickly use their current location with browser geolocation
    API on Web and native location services on React Native.
  </Card>
</Cards>

## Props

<TypeTable
  type={{
    defaultLocation: {
      description: "The default location to display",
      type: "string",
      default: "''",
    },
    autoDetectOnLoad: {
      description: "Whether to automatically detect location on component load",
      type: "boolean",
      default: "false",
    },
    variant: {
      description: "The display variant of the component",
      type: "'popover' | 'inline'",
      default: "'popover'",
    },
    placeholder: {
      description: "Placeholder text for the search input",
      type: "string",
      default: "'Enter city, district, or area'",
    },
    showLabel: {
      description:
        "Whether to show the label section (only applies to inline variant)",
      type: "boolean",
      default: "true",
    },
    onChange: {
      description: "Callback function called when location changes",
      type: "(location: string) => void",
      default: "undefined",
    },
    className: {
      description: "Additional class names for styling",
      type: "string",
      default: "''",
    },
  }}
/>

## Advanced Examples

Explore different configurations and use cases for the Location Picker component across Web and React Native platforms.

<LocationPickerAdvancedExamples />
