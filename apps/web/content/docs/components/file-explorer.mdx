---
title: File Explorer
description: A flexible grid/list component that provides seamless switching between grid and list layouts with customizable card rendering.
---

import { Code, Package, ListTree, Zap } from "lucide-react";
import FileExplorerPreview from "@/components/previews/file-explorer";
import { FileExplorerAdvancedExamples } from "@/components/previews/examples/file-explorer-advanced";

## Usage

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <FileExplorerPreview />
  </Tab>
  <Tab value="Code">
    ```tsx
import { FileExplorer } from "@/components/file-explorer"

const previewFileSystem = {
id: 'root',
name: 'my-project',
type: 'folder' as const,
expanded: true,
icon: <FolderClosed className="h-4 w-4 text-blue-500" />,
expandedIcon: <FolderOpen className="h-4 w-4 text-blue-500" />,
children: [
{
id: 'src',
name: 'src',
type: 'folder' as const,
children: [
{
id: 'file1',
name: 'example.ts',
type: 'file' as const,
language: 'typescript',
content: 'console.log("Hello world!");',
icon: <FileCode className="h-4 w-4 text-green-500" />
},
{
id: 'images',
name: 'images',
type: 'folder' as const,
icon: <FolderClosed className="h-4 w-4 text-purple-500" />,
expandedIcon: <FolderOpen className="h-4 w-4 text-purple-500" />,
children: [{
id: 'logo.png',
name: 'logo.png',
type: 'file' as const,
isImage: true,
imageUrl: 'https://via.placeholder.com/300x200/3B82F6/FFFFFF?text=Logo',
content: 'Binary image data',
icon: <ImageIcon className="h-4 w-4 text-orange-500" />
}]
}
]
}
]
}

export default function MyComponent() {
      return (
        <FileExplorer
          initialData={previewFileSystem}
          showTitle={true}
          height="400px"
          className="w-full"
        />
      )
}
```
  </Tab>
</Tabs>

## Installation

<p className="md:text-sm  text-gray-700 dark:text-gray-300 max-w-3xl">
  Install the File Explorer component using your preferred package manager.
</p>

```npm
npx shadcn@latest add @rigidui/file-explorer
```

## Features

<p className="md:text-sm mb-4!  text-gray-700 dark:text-gray-300 max-w-3xl">
  The File Explorer component is packed with powerful features designed to
  provide an exceptional user experience.
</p>

<Cards>
  <Card title="Tree Structure">
    Navigate through a hierarchical file system with expandable folders and
    files.
  </Card>
  <Card title="File Search">
    Quickly find files by name with an integrated search functionality.
  </Card>
  <Card title="Copy Content">
    Easily copy file contents to clipboard with a single click.
  </Card>
  <Card title="Syntax Highlighting">
    View file contents with language-specific syntax highlighting for better
    readability.
  </Card>
  <Card title="Image Preview">
    Preview images directly in the explorer with support for multiple formats.
  </Card>
  <Card title="Accessibility">
    Full keyboard navigation support and screen reader compatibility.
  </Card>
  <Card title="Performance Optimized">
    Memoized components and optimized rendering for large file structures.
  </Card>
  <Card title="Error Handling">
    Robust error boundaries and graceful fallbacks for better user experience.
  </Card>
</Cards>

## Props

<TypeTable
  type={{
    initialData: {
      description: "The initial file system data structure to display",
      type: "FolderType",
      default: "defaultFileSystemData",
    },
    className: {
      description: "Additional class names for the container",
      type: "string",
      default: "''",
    },
    cardClassName: {
      description: "Additional class names for the card components",
      type: "string",
      default: "''",
    },
    title: {
      description: "Title displayed above the file explorer",
      type: "string",
      default: "'File Explorer'",
    },
    showTitle: {
      description: "Whether to show the title or not",
      type: "boolean",
      default: "true",
    },
    height: {
      description: "Height of the file explorer component",
      type: "string",
      default: "'calc(100vh-200px)'",
    },
    fileContentHeight: {
      description: "Height of the file content display area",
      type: "string",
      default: "'100%'",
    },
    defaultFileIcon: {
      description: "Default icon to use for all files without a custom icon",
      type: "React.ReactNode",
      default: '<FileText className="h-4 w-4 text-muted-foreground" />',
    },
    defaultFolderIcon: {
      description:
        "Default icon to use for closed folders without a custom icon",
      type: "React.ReactNode",
      default: '<Folder className="h-4 w-4 text-yellow-500" />',
    },
    defaultFolderOpenIcon: {
      description: "Default icon to use for open folders without a custom icon",
      type: "React.ReactNode",
      default: '<FolderOpen className="h-4 w-4 text-yellow-500" />',
    },
    onFileSelect: {
      description: "Callback function called when a file is selected",
      type: "(file: FileType) => void",
      default: "undefined",
    },
    onFolderToggle: {
      description:
        "Callback function called when a folder is expanded or collapsed",
      type: "(folderId: string, isExpanded: boolean) => void",
      default: "undefined",
    },
    readOnly: {
      description: "Whether the file explorer is in read-only mode",
      type: "boolean",
      default: "false",
    },
    allowedFileTypes: {
      description: "Array of allowed file extensions for filtering",
      type: "string[]",
      default: "undefined",
    },
    maxFileSize: {
      description: "Maximum file size in bytes for validation",
      type: "number",
      default: "undefined",
    },
    loading: {
      description: "Whether the component is in a loading state",
      type: "boolean",
      default: "false",
    },
    onRefresh: {
      description: "Callback function for the refresh button",
      type: "() => void",
      default: "undefined",
    },
  }}
/>
